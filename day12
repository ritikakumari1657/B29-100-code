Q23: Write a program to calculate library fine based on late days as follows: 
#include <stdio.h>

int main() {
    int late_days;
    float fine = 0.0;

    printf("Enter number of late days: ");
    scanf("%d", &late_days);

    if (late_days <= 0) {
        printf("No fine.\n");
    } else if (late_days <= 5) {
        fine = late_days * 1.0; // Rs. 1 per day for first 5 days
        printf("Fine = Rs. %.2f\n", fine);
    } else if (late_days <= 10) {
        fine = 5 * 1.0 + (late_days - 5) * 2.0; // Rs. 2 per day for next 5 days
        printf("Fine = Rs. %.2f\n", fine);
    } else {
        fine = 5 * 1.0 + 5 * 2.0 + (late_days - 10) * 5.0; // Rs. 5 per day after 10 days
        printf("Fine = Rs. %.2f\n", fine);
    }

    return 0;
}
Q24: Write a program to calculate electricity bill based on units consumed with these rates: 
First 100 units at ₹5/unit 
Next 100 units at ₹7/unit 
Next 100 units at ₹10/unit 
Above at ₹12/unit
#include <stdio.h>

int main() {
    int late_days;
    float fine = 0.0;

    printf("Enter number of late days: ");
    scanf("%d", &late_days);

    if (late_days <= 0) {
        printf("No fine.\n");
    } else if (late_days <= 5) {
        fine = late_days * 1.0; // Rs. 1 per day for first 5 days
        printf("Fine = Rs. %.2f\n", fine);
    } else if (late_days <= 10) {
        fine = 5 * 1.0 + (late_days - 5) * 2.0; // Rs. 2 per day for next 5 days
        printf("Fine = Rs. %.2f\n", fine);
    } else {
        fine = 5 * 1.0 + 5 * 2.0 + (late_days - 10) * 5.0; // Rs. 5 per day after 10 days
        printf("Fine = Rs. %.2f\n", fine);
    }

    return 0;
}

