Q97: Print the initials of a name.

#include <stdio.h>
#include <ctype.h>
  int main() {
  char name[100];
  int i = 0;
  printf("Enter your full name: ");
  fgets(name, sizeof(name), stdin);
  printf("Initials: ");
  if(isalpha(name[0])) {
    printf("%c" , toupper(name[0]));
  }
  while(name[i] != '\0'){
    if(mame[i] == ''&& isalpha(name[i+1])) {
      printf("%c", toupper(name[i+1]));
    }
    i++;
  }
  printf("\n");
  return 0;
}
Q98: Print initials of a name with the surname displayed in full.

#include <stdio.h>
#include <ctype.h>
#include <string.h>
  int main(){
  char name[100];
  int i = 0, len;
  printf("Enter your full name: ");0
  fgets(name, sizeof(name), stdin);
  len = strlen(name);
  if(name[len-1] == '\n'){
    name[len-1] ='\n';
  }
  printf("Result:");
  if(isalpha(name[0])){
    printf("%c.",toupper (name[0]));
  }
  for (i = 0; i < len; i++){
    if(name[i] == '' && name[i + 1] != '\0'){
      int j = i+1;
      while (j < len && name[j] != '') j++;
      if (j == len){
        printf("%c.", toupper(name[i + 1]));
      }
    }
  }
  printf("\n");
  return 0;
}
