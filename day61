Q111: Write a program to take an integer array arr and an integer k as inputs. The task is to find the first negative integer in each subarray of size k moving from left to right. 
  If no negative exists in a window, print "0" for that window. Print the results separated by spaces as output.


  #include <stdio.h>
#include <stdlib.h> 
#include <stdbool.h>
void firstNegative(int arr[], int n, int k) {
    int deque[n];
    int front = 0, rear = -1;
    for (int i = 0; i < k; i++) {
        if (arr[i] < 0) {
            deque[++rear] = i;
        }
    }
  for (int i = k; i < n; i++) {
        if (front <= rear) {
            printf("%d ", arr[deque[front]]);
        } else {
            printf("0 ");
        }
        while (front <= rear && deque[front] <= i - k) {
            front++;
        }
        if (arr[i] < 0) {
            deque[++rear] = i;
        }
    if (front <= rear) {
        printf("%d\n", arr[deque[front]]);
    } else {
        printf("0\n");
    }
}
int main() {
    int arr[] = {12, -1, -7, 8, -15, 30, 16, 28};
    int n = sizeof(arr) / sizeof(arr[0]);
    int k = 3;
    printf("Input array: ");
    for (int i = 0; i < n; i++) {
        printf("%d ", arr[i]);
    }
    printf("\nWindow size (k): %d\n", k);
    printf("First negative integers in each window: ");
    firstNegative(arr, n, k);
    int arr2[] = {-8, 2, 3, -6, 10};
    int n2 = sizeof(arr2) / sizeof(arr2[0]);
    int k2 = 2;
    printf("\nInput array 2: ");
    for (int i = 0; i < n2; i++) {
        printf("%d ", arr2[i]);
    }
    printf("\nWindow size (k): %d\n", k2);
    printf("First negative integers in each window 2: ");
    firstNegative(arr2, n2, k2);
    return 0;
}
