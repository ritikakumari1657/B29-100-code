Q27: Write a program to print the sum of the first n odd numbers.
#include<stdio.h>
int main(){
    int n , i, sum=0, odd=1;
    printf("enter the value of n:");
    scanf("%d", &n);
    printf("first %d odd numbers are:", n);
    for(i=1;i<=n;i++){
        printf("%d ", odd);
        sum += odd;
        odd += 2;
    }
    printf("\nsum of first %d odd numbers is: %d", n, sum);

return 0;
}
Q28: Write a program to print the product of even numbers from 1 to n.
#include<stdio.h>
int main(){
    int n, i;
    long long product = 1;
    printf("enter the value of n:");
    scanf("%d", &n);
    for(i=1; i<=n; i++){
        product *= i;
    }
    printf("product of first %d numbers is: %lld", n, product);
    return 0;
}
